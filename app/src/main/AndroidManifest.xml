<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jobresearch.controller.jobsresearch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InscriptionActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ConnexionActivity" />
        <activity android:name=".DemandeurActivity" />
        <activity android:name=".RecruteurActivity" />
        <activity android:name=".CvActivity" />
        <activity android:name=".OffreActivity" />
        <activity android:name=".ListCvActivity" />
        <activity android:name=".UpdateCv" />
        <activity android:name=".DeleteCvActivity" />
        <activity
            android:name=".Activity"
            android:label="@string/title_activity_"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>